.index-heading RFCs

table.index-table
  thead
    tr
      th Software Release
      th RFC
      th Description
      th Project
      th Delete

  tbody
    - @rfcs.each do |rfc|
      tr
        td = index_link rfc.try(:milestone).try(:software_release).try(:name) , rfc
        td = index_link rfc.rfc                                               , rfc
        td = index_link rfc.description                                       , rfc
        td = index_link rfc.try(:milestone).try(:project).try(:name)          , rfc
        td = delete_link rfc

.row

= new_button
